<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PharmacyMobile.Pages.NewsPage">
  <ListView x:Name="MyListView"
            ItemsSource="{Binding NewsItems}"
            CachingStrategy="RecycleElement"
            HasUnevenRows="True" SeparatorColor="#FF76E383" ItemTapped="Handle_ItemTapped">

        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout Orientation="Vertical" Padding="10">
                        <StackLayout Orientation="Horizontal">
                            <Label Text="{Binding Title}" Style="{StaticResource LabelTitle}"/>
                            <Label Text="{Binding Date}" MinimumWidthRequest="100" HorizontalTextAlignment="End"/>
                        </StackLayout>
                        <Label Text="{Binding Text}" Style="{StaticResource LabelText}"/>
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
       
    </ListView>
</ContentPage>
